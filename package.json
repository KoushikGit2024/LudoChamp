{
  "name": "ludochamp",
  "private": true,
  "scripts": {
    "start": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "type": "commonjs",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
